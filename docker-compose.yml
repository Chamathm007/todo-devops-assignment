services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: deps
    container_name: todo-web
    working_dir: /app
    command: npm run dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development